# Figures

The three PNGs here are the upstream paper's figures, referenced by the
repo README. WP-era result plots were removed in the live-arms cleanup —
all recoverable from git history, all regenerable from the CSVs archived
per run (see docs/STATS.md for run IDs).
